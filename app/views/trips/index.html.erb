<h1>Here are all your upcoming trips</h1>

<table>
  <% @trips.each do |trip| %>
  <tr>
    <td><%= trip.destination %></td>
    <td><%= link_to 'Show', trip_path(trip) %></td>
  </tr>
  <% end %>
</table>
